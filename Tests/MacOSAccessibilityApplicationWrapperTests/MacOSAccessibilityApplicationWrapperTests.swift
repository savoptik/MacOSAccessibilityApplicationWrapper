import XCTest
@testable import MacOSAccessibilityApplicationWrapper

final class MacOSAccessibilityApplicationWrapperTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(MacOSAccessibilityApplicationWrapper().text, "Hello, World!")
    }
}
